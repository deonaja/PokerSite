// localStorage key marking that this device has seen the /identity first-run
// welcome fork (Mulai tur / Aku pemain, langsung masuk). Per-device (not
// per-user), mirrors the changelog "seen" pattern.
export const GUIDE_SEEN_KEY = 'panduan_seen'
