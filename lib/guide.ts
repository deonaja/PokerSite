// localStorage key marking that this device has seen the onboarding guide /
// welcome sheet. Per-device (not per-user), mirrors the changelog "seen" pattern.
export const GUIDE_SEEN_KEY = 'panduan_seen'
